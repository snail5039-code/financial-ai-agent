# 금융 AI 에이전트 프로젝트 인수인계

> 작성일: 2026-08-26 KST  
> 작업공간: `C:\Users\snail\OneDrive\바탕 화면\new_idea`  
> 저장소: https://github.com/snail5039-code/financial-ai-agent  
> 기본 브랜치: `main`

## 1. 프로젝트 목적

금융 AI의 제안, 독립 검증, 사용자 승인 과정을 투명하게 보여주는 macOS 스타일 데스크톱 목업을 제작한다. 현재 단계는 **화면과 로컬 인터랙션 검증**이며 실제 금융 데이터, 계좌, 주문, AI 실행, API 및 DB는 연결하지 않는다.

모든 담당자는 루트의 `AGENTS.md`를 최우선으로 따른다. 표준 순서는 `기획자 → 관리자 승인 → 구현자 → 독립 검증자 → 재작업 → 관리자 인수`이다.

## 2. 역할별 Codex 작업

- 관리자: `금융 AI 에이전트 관리자 2세대`
  - 작업 ID: `01a03b94-8c57-7fc2-8e5e-d96556268db7`
- 기획자: `금융 AI 에이전트 구현 기획자 2세대`
  - 작업 ID: `01a03b95-8efb-7212-bd49-b2be3e672f4d`
- 구현자: `금융 AI 에이전트 구현자 2세대`
  - 작업 ID: `01a03b95-e0ef-7371-9842-8e103ec60226`
- 검증자: `금융 AI 에이전트 검증자 2세대`
  - 작업 ID: `01a03b95-f5f0-7901-bcca-e8089df8ab27`
- 관리자: `금융 AI 에이전트 관리자 3세대`
  - 작업 ID: `01a03c51-e051-7ca2-ba59-d1e442de2e7c`
- 기획자: `금융 AI 에이전트 구현 기획자 3세대`
  - 작업 ID: `01a03c52-07b8-7322-b047-86b35b63cad1`
- 구현자: `금융 AI 에이전트 구현자 3세대`
  - 작업 ID: `01a03c52-33aa-7890-9d14-eb4708a4834a`
- 검증자: `금융 AI 에이전트 검증자 5세대`
  - 작업 ID: `01a03c52-60d4-7803-a7c8-9e24087aa79a`
- 검증자: `금융 AI 에이전트 검증자 6세대`
  - 작업 ID: `01a03cb1-3575-7310-bb31-5fcb02b9e59b`

관리자는 각 작업에 작업 ID가 포함된 지시를 보내 결과를 취합한다. 구현자가 자체 승인하거나 검증자가 직접 구현물을 수정하면 안 된다.

### 새 관리자 세대의 사이드바 작업 생성

- 현재 작업들의 컨텍스트가 많이 찼으므로 다음 관리자는 기존 역할 작업을 계속 재사용하지 않는다.
- 관리자·기획자·구현자·검증자 중 어느 작업이든 **컨텍스트 사용량이 50%에 도달하면 해당 역할의 새 세대 작업을 생성해 인수인계한다.** 50%를 넘긴 뒤까지 장시간 작업을 계속하지 않는다.
- 컨텍스트 사용률을 직접 확인할 수 있으면 표시된 수치를 기준으로 한다.
- 정확한 사용률을 확인할 수 없으면 다음 중 하나가 나타날 때 50% 도달로 보수적으로 간주한다.
  - 대화가 여러 차례 요약되거나 이전 지시가 압축됨
  - 이미 완료한 작업을 다시 확인하거나 역할·작업 ID를 혼동함
  - 긴 도구 출력 때문에 핵심 요구사항이 누락될 위험이 커짐
  - 사용자가 컨텍스트가 많이 찼다고 알림
- 50% 도달 시 진행 중인 위험한 외부 행동을 새로 시작하지 않는다. 현재의 안전한 로컬 단계를 정리하고 `HANDOFF.md`를 최신화한 뒤 새 작업으로 넘긴다.
- 인수인계에는 현재 목표, 완료 항목, 미완료 항목, 변경 파일, Git 상태, 테스트 결과, 위험, 정확한 다음 행동을 반드시 기록한다.
- 새 작업은 사이드바에서 사용자가 직접 볼 수 있게 만들고, 이름의 세대 번호를 1 올린다.
- 새 작업이 `AGENTS.md`와 `HANDOFF.md`를 읽고 인수 사실을 보고한 뒤에만 이전 작업의 책임을 넘긴다.
- 역할 하나만 50%에 도달하면 해당 역할만 새 세대로 교체할 수 있다. 관리자가 50%에 도달하면 지휘 연속성을 위해 관리자 작업을 우선 교체하고, 다른 역할들의 잔여 컨텍스트도 함께 점검한다.
- **새 관리자가 지정되면 관리자·기획자·구현자·검증자 4개 작업을 새 세대로 만들어야 한다.**
- 네 작업은 숨은 임시 에이전트가 아니라 Codex 사이드바에서 사용자가 직접 열어볼 수 있는 별도 작업이어야 한다.
- 권장 작업명은 세대를 구분할 수 있도록 다음과 같이 정한다.
  - `금융 AI 에이전트 관리자 2세대`
  - `금융 AI 에이전트 구현 기획자 2세대`
  - `금융 AI 에이전트 구현자 2세대`
  - `금융 AI 에이전트 검증자 2세대`
- 이후 다시 컨텍스트가 찰 경우 `3세대`, `4세대`처럼 번호를 올린다.
- 이전 세대 작업은 삭제하지 않고 기록·감사·근거 확인용으로 남긴다. 사용자가 요청할 때만 보관 처리한다.
- 새 관리자 작업을 시작하는 첫 메시지는 다음 내용을 포함해야 한다.
  - `AGENTS.md`와 `HANDOFF.md` 전체 읽기
  - 금융 AI 에이전트 관리자로서 새 역할 작업 3개 생성 및 지휘
  - 미완료 작업부터 이어서 진행
  - 실제 금융 행동과 외부 변경의 승인 경계 준수
- 새 기획자·구현자·검증자의 첫 메시지도 `AGENTS.md`와 `HANDOFF.md` 전체를 읽고 자신의 역할만 수행하도록 명시한다.
- 토큰 소비 절감을 위해 새 관리자와 구현 기획자 작업은 기본 모델을 `GPT-5.5`로 지정한다. 실행 중인 작업의 모델을 바꿀 수 없으면 다음 후속 지시 또는 다음 세대부터 적용한다.
- 역할 작업은 승인 팝업이 필요한 행동을 먼저 관리자에게 보고하고, 관리자가 승인·실행 지시 또는 관리자 작업에서의 대체 실행을 담당한다. 시스템이 사용자에게만 요구하는 보안 승인은 우회하지 않는다.
- 새 관리자는 새 기획자·구현자·검증자 작업에 직접 후속 지시를 보내고 결과를 기다린 뒤 다음 관문으로 넘긴다.
- 새 작업 4개의 ID가 발급되면 아래 `현재 활성 세대` 표와 이 문서 상단 역할 목록을 즉시 갱신한다.
- 새 작업 생성은 사용자에게 직접 보이는 외부 작업 생성이므로 사용자의 요청 또는 승인을 받은 뒤 수행한다. 현재 사용자는 이 세대교체 절차를 명시적으로 승인했다.

### 현재 활성 세대

| 역할 | 세대 | 작업명 | 작업 ID | 상태 |
|---|---:|---|---|---|
| 관리자 | 1 | 금융 AI 에이전트 관리자 설정 | 현재 작업 | 이전 세대 |
| 기획자 | 1 | 금융 AI 에이전트 구현 기획자 | `01a03820-ea5c-7571-8a10-1550d4461a18` | 이전 세대 |
| 구현자 | 1 | 금융 AI 에이전트 구현자 | `01a03836-699d-7780-b7d8-a42b8be7cc0d` | 이전 세대·컨텍스트/사용량 제한 발생 |
| 검증자 | 1 | 금융 AI 에이전트 검증자 | `01a03836-f0d6-7822-9ad8-82fac37d639a` | 이전 세대 |
| 관리자 | 2 | 금융 AI 에이전트 관리자 2세대 | `01a03b94-8c57-7fc2-8e5e-d96556268db7` | 이전 세대·컨텍스트 50% 초과 |
| 기획자 | 2 | 금융 AI 에이전트 구현 기획자 2세대 | `01a03b95-8efb-7212-bd49-b2be3e672f4d` | 이전 세대·3세대 인수 |
| 구현자 | 2 | 금융 AI 에이전트 구현자 2세대 | `01a03b95-e0ef-7371-9842-8e103ec60226` | 이전 세대·3세대 인수 |
| 검증자 | 2 | 금융 AI 에이전트 검증자 2세대 | `01a03b95-f5f0-7901-bcca-e8089df8ab27` | 브라우저 도구 시스템 승인 대기 |
| 검증자 | 3 | 금융 AI 에이전트 검증자 3세대 | `01a03bac-da7d-7492-97e4-9d4b299befb9` | MOCKUP-005-V2 통과 |
| 검증자 | 4 | 금융 AI 에이전트 검증자 4세대 | `01a03c49-727a-7ce2-9677-1c5f3880fa60` | MOCKUP-007-V4-R1 통과 |
| 관리자 | 3 | 금융 AI 에이전트 관리자 3세대 | `01a03c51-e051-7ca2-ba59-d1e442de2e7c` | 활성·인수 완료 |
| 기획자 | 3 | 금융 AI 에이전트 구현 기획자 3세대 | `01a03c52-07b8-7322-b047-86b35b63cad1` | 활성 |
| 구현자 | 3 | 금융 AI 에이전트 구현자 3세대 | `01a03c52-33aa-7890-9d14-eb4708a4834a` | MOCKUP-011 구현 완료 |
| 검증자 | 5 | 금융 AI 에이전트 검증자 5세대 | `01a03c52-60d4-7803-a7c8-9e24087aa79a` | MOCKUP-008-V 실패·V-R1 사용량 제한 |
| 검증자 | 6 | 금융 AI 에이전트 검증자 6세대 | `01a03cb1-3575-7310-bb31-5fcb02b9e59b` | MOCKUP-008-V6-R1 통과 |

## 3. 완료 및 원격 반영된 작업

### 최초 공개

- 커밋: `ae2d916`
- 메시지: `금융 AI 에이전트 목업과 기획 문서 추가`
- 공개 저장소와 한국어 README 생성

### 승인 대기·주문 상세

- 커밋: `25202a1`
- 메시지: `승인 대기 및 주문 상세 목업 추가`

### 감사 로그·검증 비교

- 커밋: `2c4eea3`
- 메시지: `에이전트 실행 기록과 검증 비교 화면 추가`
- 첨부 피드백에서 발견된 타임라인 칸 이탈 문제도 수정됨
- 타임라인은 `버튼-선-버튼-선-버튼` 그리드이며 아이콘과 문구가 버튼 내부에 제한됨

### 투자 정책 설정

- 커밋: `118a4de`
- 메시지: `투자 정책 설정 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 주간 투자 리포트

- 커밋: `64ad192`
- 메시지: `주간 투자 리포트 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 인수인계 문서 정리

- 커밋: `ba978eb`
- 메시지: `인수인계 문서 현재 상태 정리`
- `main`과 `origin/main`에 반영됨

### 데이터 연결·출처 상태

- 커밋: `1bfa662`
- 메시지: `데이터 연결 상태 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 3세대 역할 작업 인수인계

- 커밋: `33f3a3a`
- 메시지: `3세대 역할 작업 인수인계 기록`
- `main`과 `origin/main`에 반영됨

### 모의 거래 내역

- 커밋: 완료 커밋
- 메시지: `모의 거래 내역 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 리스크 알림·차단 이벤트

- 커밋: 완료 커밋
- 메시지: `리스크 알림 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 알림 수신 설정

- 커밋: 완료 커밋
- 메시지: `알림 수신 설정 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 백테스트·전략 검증 요약

- 커밋: 완료 커밋
- 메시지: `백테스트 전략 검증 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

### 전략 비교·리밸런싱 제안

- 커밋: 완료 커밋
- 메시지: `전략 조정 리밸런싱 화면 목업 추가`
- `main`과 `origin/main`에 반영됨

현재 로컬 `main`과 `origin/main`은 완료 커밋에서 일치해야 한다.

## 4. 현재 구현된 화면

위치: `mockup/financial-dashboard/`

1. `index.html` — 투자 운영 대시보드
   - 총자산, 성과 차트, 보유 종목, AI 제안·검증·모의승인
2. `company-detail.html` — 삼성전자 기업 상세·공시 근거
   - 가상 지표, 긍정·반대 근거, 미연결 공시, 검증 인스펙터
3. `approval-queue.html` — 승인 대기 목록·주문 상세
   - 가상 요청 4건, 필터, 독립 상태, 반려·모의승인
4. `audit-log.html` — 에이전트 실행 기록·검증 비교
   - 결정 3건, 처리 타임라인, 최초 제안/검증 후 결과, 출처 상태
5. `policy-settings.html` — 투자 정책 설정
   - 위험 한도, 차단 규칙, DEC-1042 가상 적용 미리보기
6. `weekly-report.html` — 주간 투자 리포트
   - 자산·손익 변화, 벤치마크 비교, 에이전트 처리 기록, 주요 위험과 데이터 한계
7. `data-connections.html` — 데이터 연결·출처 상태
   - 공시·가격·증권사·DB 연결의 미연결 상태와 출처 품질 점검
8. `trade-history.html` — 모의 거래 내역
   - 기간·상태 필터, 가상 제안 이력, 선택 이력 인스펙터, 비용·정책·출처 한계
9. `risk-alerts.html` — 리스크 알림·차단 이벤트
   - 심각도·카테고리 필터, 가상 위험 원인, 권장 확인, 관련 화면 링크
10. `notification-settings.html` — 알림 수신 설정
   - 채널 선택, 알림 유형 토글, 심각도 기준, 조용한 시간, 화면용 테스트 미리보기
11. `backtest-summary.html` — 백테스트·전략 검증 요약
   - 기간·전략 필터, 기간별 가상 지표, 월별 결과, 계산·비용 가정과 편향·과최적화 한계
12. `rebalance-plan.html` — 전략 비교·리밸런싱 제안
   - 전략별 목표 비중, 현재·목표 비중 비교, 화면용 조정 제안, 정책·리스크·출처 점검

각 완료 화면에는 대응하는 `*-1440x900.png` 캡처가 있다.

## 5. 공통 화면 규칙

- 기준 화면: `1440×900px`
- 앱 창: `1392×852px`, 외부 여백 24px
- 타이틀바: 52px
- 내부 열: `223px / 815px / 352px`
- 라이트 모드, 얇은 구분선, 시스템 글꼴
- 큰 KPI 카드, 네온, 과도한 그림자·그라데이션, AI 채팅 UI 금지
- 선택과 경고는 색상뿐 아니라 텍스트·아이콘·ARIA 상태로 표현
- 모든 금융 수치와 출처는 화면용 가상 예시이며 실제 데이터로 오인되지 않게 표시

## 6. 현재 미커밋 작업

현재 미커밋 작업은 없다. 로컬 `main`과 `origin/main`은 완료 커밋에서 일치해야 한다.

## 7. 투자 정책 화면 현재 상태

구현 파일과 캡처가 생성되었고 `MOCKUP-005-I-R1` 재작업을 원본 작업공간에 반영했다. 숫자 step 검증, DEC-1042의 금액·비중·지정가·출처 통합 판정, 위험 경고, `aria-current`, 우측 패널 겹침을 보완했다. 관리자 브라우저 점검에서는 1440×900 레이아웃, 숫자 경계값 18건, 위험 경고 4종, 적용 상태, 기존 4개 화면의 링크·가로 오버플로·콘솔 오류 부재를 확인했다. 검증자 2세대의 브라우저 도구가 시스템 승인 대기에 들어가 관리자 승인 지시를 추가했으나 대기가 해소되지 않았다. 승인 대기 방치를 피하기 위해 검증자 3세대 `01a03bac-da7d-7492-97e4-9d4b299befb9`를 만들고, 브라우저 승인 팝업 없이 원본 파일 정적 검증과 관리자 증거 대조로 독립 판정하도록 지시했다. 검증자 3세대는 `MOCKUP-005-V2`를 `통과`로 판정했으며 중대·높음·보통 문제는 발견하지 않았다. 낮음 이슈로 HANDOFF 세대 정보 갱신 필요만 보고했고 본 문서에 반영했다.

고정 정책값:

- 종목별 최대 비중: 8.0% (`1~30`)
- 1회 최대 주문금액: 1,000,000원 (`100,000~10,000,000`)
- 일일 최대 손실률: 3.0% (`0.5~10`)
- 최소 현금 비중: 15.0% (`0~50`)
- 최근 20일 변동성 경계: 28.0% (`5~80`)
- 승인 만료: 10분 (`1~60`)
- 지정가 허용, 시장가 미허용
- 출처 미확인 자동 차단 켬
- 정정 공시 미확인 자동 차단 켬

DEC-1042 가상 적용 미리보기:

- `10주 × 71,200원 = 712,000원`
- 비중 `6.65% → 7.20%`
- 지정가
- 출처 미확인
- 기본 정책에서는 `차단 예시`

## 8. 주간 투자 리포트 완료 상태

`MOCKUP-006-I` 구현과 `MOCKUP-006-I-R1` 재작업이 완료되었고 `MOCKUP-006-V-R1` 독립 재검증은 `통과`였다. 신규 파일은 `weekly-report.html`, `weekly-report.css`, `weekly-report.js`, `weekly-report-1440x900.png`이며 기존 화면의 투자 리포트 링크를 `weekly-report.html`로 연결했다.

관리자 브라우저 점검에서는 1440×900 앱 창 1392×852, 본문 815px, 우측 인스펙터 352px, 가로 오버플로 없음, 콘솔 오류 없음, 정책 차단 선택 시 인스펙터 갱신, 기간 선택 `aria-pressed` 갱신을 확인했다.

검증자 3세대의 1차 `MOCKUP-006-V` 판정은 `실패`였다. 높음 문제는 기간 버튼을 `1개월`로 바꿔도 주간 기간·손익·수익률이 유지되는 점이었고, 보통 문제는 현금 비중의 총자산 기준이 리포트 종료 총자산과 다른 점이었다. 재작업 후 기간 선택 시 날짜, 시작·종료 총자산, 손익, 포트폴리오 수익률, 벤치마크, 초과수익, 막대, 계산식, 인스펙터 근거가 함께 갱신된다. 현금 비중 근거는 종료 총자산 130,180,000원과 현금성 자산 23,953,000원 기준 18.4%로 통일했다.

## 9. 데이터 연결·출처 상태 완료 상태

`MOCKUP-007-I` 구현과 재작업이 완료되었고 `MOCKUP-007-V4-R1` 독립 재검증은 `통과`였다. 신규 파일은 `data-connections.html`, `data-connections.css`, `data-connections.js`, `data-connections-1440x900.png`이며 대시보드, 주간 리포트, 투자 정책 화면의 `데이터 연결` 메뉴를 `data-connections.html`로 연결했다.

- 실제 API, DB, 계좌, 주문 연결은 하지 않는다.
- OpenDART, 증권사 API, 가격 데이터, 공시 데이터, 계좌 연결은 모두 `미연결` 또는 `화면용 가상 상태`로 표시한다.
- 연결 상태, 권한 범위, 마지막 확인 시각, 데이터 한계, 출처 미확인 경고를 한 화면에서 확인할 수 있게 한다.
- 기존 화면과 같은 1440×900, 223/815/352 3열 구조와 macOS 라이트 목업 톤을 유지한다.

관리자 점검에서 `node --check data-connections.js` 통과, 로컬 URL `http://127.0.0.1:4173/data-connections.html` 응답 200, 금지 키워드 `fetch`, `XMLHttpRequest`, `WebSocket`, `EventSource`, `sendBeacon`, `localStorage`, `sessionStorage`, `indexedDB`, `document.cookie`, `action=`, 외부 URL 없음, 1440×900 앱 창 1392×852, 본문 815px, 우측 인스펙터 352px, 가로 오버플로 없음, 콘솔 오류 없음, `증권사` 선택과 `차단` 필터 및 `화면용 연결 점검` 상태 문구 갱신을 확인했다. 독립 검증은 아직 수신 전이다.

검증자 4세대의 `MOCKUP-007-V4` 1차 판정은 `실패`였다. 높음 문제는 가상 항목 선택 후 `차단` 필터를 누르면 숨겨진 항목의 선택 상태와 인스펙터가 유지되는 점이었다. 보통 문제는 필터명이 차단 상태만 뜻하는 것처럼 보이는 점과 `data-connections-1440x900.png` 파일 헤더가 JPEG인 점이었다. 낮음 문제는 루트 README 캡처 목록에 데이터 연결 캡처가 빠진 점이었다. 재작업으로 필터명을 `차단·미연결`로 바꾸고, 필터 적용 후 숨겨진 선택 항목이 있으면 보이는 첫 항목으로 자동 재선택되도록 수정했다. `source-row`의 잘못된 `role="row"`를 제거해 버튼 접근성을 복구했고, 캡처 파일을 실제 PNG 헤더로 변환했으며 README 캡처 목록을 보완했다. 관리자 재점검에서 `가격 데이터` 선택 후 `차단·미연결` 필터를 누르면 선택 항목이 보이는 `OpenDART 공시`로 바뀌고 인스펙터도 `OpenDART 공시`로 갱신되며, 가로 오버플로와 콘솔 오류가 없음을 확인했다. 검증자 4세대의 `MOCKUP-007-V4-R1` 재검증은 `통과`이며 남은 중대·높음·보통·낮음 문제는 없다.

## 10. 다음 제품 단계

**모의 거래 내역 화면**은 `MOCKUP-008`로 완료되었다.

- 실제 주문·계좌·API·DB 연결 없이 화면용 가상 거래/제안 이력만 표시한다.
- 상태는 `모의승인`, `반려`, `정책 차단`, `만료`, `대기`처럼 실거래와 분리해 표시한다.
- 기간/상태 필터, 이력 목록, 선택 이력 인스펙터, 감사 로그·주간 리포트·정책 화면 연결을 포함한다.
- 체결가, 수량, 주문금액, 수수료·세금·슬리피지 한계는 모두 가상 예시임을 표시한다.
- 기존 화면과 같은 1440×900, 223/815/352 3열 구조와 macOS 라이트 목업 톤을 유지한다.

검증 흐름:

- 기획자 3세대 `MOCKUP-008-P`: 계획 승인
- 구현자 3세대 `MOCKUP-008-I`: 구현 완료
- 검증자 5세대 `MOCKUP-008-V`: 실패
  - 높음: 이력별 정책·출처 정보 미갱신
  - 보통: 감사 로그·투자 정책 화면의 거래 내역 링크 누락
- 구현자 3세대 `MOCKUP-008-I-R1`: 재작업 완료
  - `policyResult`, `sourceState`를 이력별로 분리하고 인스펙터에 반영
  - `audit-log.html`, `policy-settings.html`에 거래 내역 링크 추가
- 검증자 6세대 `MOCKUP-008-V6-R1`: 통과
  - 중대·높음·보통·낮음 문제 없음

**리스크 알림·차단 이벤트 화면**은 `MOCKUP-009`로 완료되었다.

- 실제 금융 연결 없이 화면용 가상 위험 이벤트 6건만 표시한다.
- 심각도 필터 `전체/중대/높음/보통/낮음`과 카테고리 필터 `전체/정책/출처/시장/승인/데이터`를 제공한다.
- 선택 이벤트 인스펙터에서 원인, 권장 사용자 확인, 관련 정책, 데이터 미연결, 외부 요청 0건, 실제 주문·계좌 없음을 표시한다.
- 정책 설정·거래 내역·감사 로그·데이터 연결 화면으로 이동할 수 있다.
- 관리자 점검에서 `node --check risk-alerts.js` 통과, 금지 연결·저장소 키워드 0건, 1440×900 앱 창 1392×852와 내부 열 223/815/352, 가로 오버플로 없음, 콘솔 오류 없음, 필터·빈 상태·자동 재선택을 확인했다.
- 검증자 6세대 `MOCKUP-009-V`: 통과
  - 중대·높음·보통 문제 없음
  - 낮음 이슈였던 빈 결과 기본 링크 잔존은 관리자 정리로 해소했다.

**알림 수신 설정 화면**은 `MOCKUP-010`으로 완료되었다.

- 실제 알림 발송, 브라우저 권한 요청, 이메일·메신저 연결 없이 화면용 설정만 표시한다.
- 채널은 앱 내부, 브라우저, 이메일, 메신저로 나누고 연결 상태를 `앱 내부만`, `권한 요청 없음`, `미연결`로 표시한다.
- 알림 유형 토글, 심각도 기준 `중대만/높음 이상/보통 포함`, 조용한 시간 `22:00-08:00`, 화면용 테스트 미리보기를 제공한다.
- 테스트 미리보기는 우측 카드만 갱신하며 실제 발송이나 권한 요청을 하지 않는다.
- 관리자 점검에서 `node --check notification-settings.js` 통과, 금지 연결·저장·권한 요청 키워드 0건, 1440×900 앱 창 1392×852와 내부 열 223/815/352, 가로 오버플로 없음, 콘솔 오류 없음, 채널 선택·유형 토글·심각도 변경·테스트 미리보기와 기존 주요 화면 9개의 진입 링크를 확인했다.
- 검증자 6세대 `MOCKUP-010-V`: 통과
  - 중대·높음·보통 문제 없음
  - 낮음 이슈였던 `중대만` 표시 문구 불일치는 관리자 정리로 해소했다.

**백테스트·전략 검증 요약 화면**은 `MOCKUP-011`로 완료되었다.

- 실제 과거 데이터, 백테스트 엔진, API, DB, 계좌, 주문 연결 없이 화면용 고정 예시만 표시한다.
- 기간 필터 `3개월/6개월/1년`과 전략 필터 `보수형/균형형/공격형`을 제공한다.
- 요약 지표는 검증 기간, 포트폴리오 수익률, 벤치마크, 최대 낙폭, 변동성, 승률로 구성한다.
- 월별 결과 선택 시 우측 인스펙터의 계산식, 비용 가정, 위험 지표가 함께 갱신된다.
- 미래정보 사용 금지, 생존편향, 선택편향, 과최적화, 수수료·세금·슬리피지 한계를 명시한다.
- 관리자 점검에서 `node --check backtest-summary.js` 통과, 금지 연결·저장 키워드 0건, 1440×900 앱 창 1392×852와 내부 열 223/815/352, 가로 오버플로 없음, 콘솔 오류 없음, `1년`·`공격형` 선택과 결과 행 선택에 따른 요약·지표·인스펙터 갱신, 기존 주요 화면 10개의 진입 링크를 확인했다.
- 독립 검증: 통과
  - 중대·높음·보통·낮음 문제 없음
  - 검증자 6세대 사이드바 작업은 MOCKUP-011 검증 턴이 완료됐으나 관리자 읽기 결과 본문이 비어, 별도 읽기 전용 독립 검증으로 파일 존재, PNG 헤더, JS 문법, 금지 패턴, 1440×900 레이아웃, 상호작용, 링크, 안전 문구를 대체 확인했다.

**전략 비교·리밸런싱 제안 화면**은 `MOCKUP-012`로 완료되었다.

- 실제 계좌, 주문, 체결, API, DB 연결 없이 화면용 고정 예시만 표시한다.
- 목표 전략 `보수형/균형형/공격형` 선택에 따라 예상 수익, 변동성, 최대 낙폭, 현금 목표, 현재·목표 비중, 제안 목록이 함께 갱신된다.
- 제안 행 선택 시 우측 인스펙터의 예상 효과, 정책 점검, 리스크·출처 상태가 갱신된다.
- 승인 대기 화면 연결은 이동 링크일 뿐 실제 주문 생성이나 계좌 반영을 하지 않는다.
- 관리자 점검에서 `node --check rebalance-plan.js` 통과, 금지 연결·저장 키워드 0건, 1440×900 앱 창 1392×852와 내부 열 223/815/352, 가로 오버플로 없음, 콘솔 오류 없음, `공격형` 선택과 제안 행 선택에 따른 요약·비중·인스펙터 갱신, 기존 주요 화면 11개의 진입 링크를 확인했다.
- 독립 검증: 통과
  - 중대·높음·보통·낮음 문제 없음
  - 기존 사이드바 검증자 6세대는 MOCKUP-012 검증 턴이 완료됐으나 관리자 읽기 결과 본문이 비어, 별도 읽기 전용 독립 검증으로 파일 존재, PNG 헤더와 크기, JS 문법, 금지 패턴, 1440×900 레이아웃, 전략·제안 선택 상호작용, 링크, 안전 문구를 대체 확인했다.

다음 제품 단계는 사용자 또는 관리자가 새 화면 범위를 정한 뒤 `MOCKUP-013`으로 시작한다.

## 11. 로컬 실행

```powershell
cd "C:\Users\snail\OneDrive\바탕 화면\new_idea\mockup\financial-dashboard"
python -m http.server 4173 --bind 127.0.0.1
```

브라우저 주소:

- 대시보드: `http://127.0.0.1:4173/index.html`
- 기업 상세: `http://127.0.0.1:4173/company-detail.html`
- 승인 대기: `http://127.0.0.1:4173/approval-queue.html`
- 감사 로그: `http://127.0.0.1:4173/audit-log.html`
- 투자 정책: `http://127.0.0.1:4173/policy-settings.html`
- 주간 투자 리포트: `http://127.0.0.1:4173/weekly-report.html`
- 데이터 연결: `http://127.0.0.1:4173/data-connections.html`
- 모의 거래 내역: `http://127.0.0.1:4173/trade-history.html`
- 리스크 알림: `http://127.0.0.1:4173/risk-alerts.html`
- 알림 설정: `http://127.0.0.1:4173/notification-settings.html`
- 백테스트 요약: `http://127.0.0.1:4173/backtest-summary.html`
- 전략 조정: `http://127.0.0.1:4173/rebalance-plan.html`

## 12. 안전 및 운영 주의

- 실제 주문·매수·매도·송금·계좌 설정은 사용자 명시 승인 없이는 절대 실행하지 않는다.
- 현재 모든 화면은 시뮬레이션이며 실제 금융 판단에 사용할 수 없다.
- 비밀키, 토큰, 개인정보를 문서·커밋·화면에 넣지 않는다.
- 공개 검색이나 신규 스킬이 필요하면 후보의 출처·권한·설치 영향을 사용자에게 먼저 보고하고 승인받는다.
- 현재 별도의 신규 스킬 설치는 없다.
- 이 프로젝트에서 사용자가 `커밋`을 지시하면 별도 반대 지시가 없는 한 `commit`과 `push origin main`까지 완료하는 뜻으로 해석한다.
